

@model Category

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="d-flex align-items-center">
                <h1 class="mt-3 mb-3">Modifica Categoria</h1>
            </div>
            <form class="row g-3" asp-controller="Categoria" asp-action="Update" asp-route-id="@Model.Id" method="post">
                @Html.AntiForgeryToken()
                <div class="col-md-12">
                    <strong>Nome:</strong>
                    <input asp-for="Name" class="d-block w-100" />
                    <span asp-validation-for="Name" class="text-warning"></span>
                </div>
                <div class="col-12">
                    <strong>Descrizione:</strong>
                    <textarea asp-for="Description" rows="10" class="d-block w-100"></textarea>
                    <span asp-validation-for="Description" class="text-warning"></span>
                </div>
               
                <div class="col-12">
                    <button type="submit" class="btn btn-primary">Modifica Categoria</button>
                </div>
            </form>

        </div>
    </div>
</div>